# Use the upstream webtop image as the base
FROM lscr.io/linuxserver/webtop:debian-xfce

# Copy the injection scripts from your repository into the container
# (Ensure the 'webtop-inject-scripts' directory exists in the repo root)
COPY webtop-inject-scripts/ /custom-cont-init.d/
